[project]
name = "web"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.136.1",
  "jinja2",
  "loghelper",
  "markdown",
  "python-multipart",
  "uvicorn",
]

[tool.uv]
package = false

[tool.uv.sources]
loghelper = { workspace = true }
